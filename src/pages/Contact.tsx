import React, {FC} from 'react';
import SearchClient from "../components/SearchClient";

const Contact: FC = () => {
    return (
        <div>
            <SearchClient/>
        </div>
    );
};

export default Contact;